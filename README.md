# reqlens-examples

Example apps for testing Reqlens packages.

Express demo:

```bash
npm install
npm run dev:express
```

Open `http://localhost:4000` to use the test frontend.

Routes:

- `GET /ok`
- `GET /users/:id`
- `GET /error`
- `GET /slow`
